# a) Use a biblioteca math para calcular a raiz quadrada de um número.

import math

numero = float(input('Digite um número: '))

raiz_quadrada = math.sqrt(numero)

print(f'A raiz quadrada de {numero} é {raiz_quadrada}.')